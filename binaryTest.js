const assert = require('chai').assert;
const result = require('../dataStructure_JS/binarySTree');//result


describe('Binary Tree testcase', function () {

  it('Node should not be null', function () {
    let res = result;
    assert.isFalse(res==isNaN);
  });

  it('Nodes should be greater than three', function () {
    let res1 = result;
    assert.isTrue(res1 >= 2);
  });
  it('Nodes should not be zero', function () {
    let res1 = result;
    assert.isTrue(res1==0);
  });

  

});
